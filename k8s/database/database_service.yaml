apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: tic-tac-toe
  labels:
    app: database
spec:
  selector:
    app: database
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  type: ClusterIP
